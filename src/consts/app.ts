import { Platform } from 'react-native';

export const isIos = Platform.OS === 'ios';
export const isAndroid = Platform.OS === 'android';

export const APP_PADDING_HORIZONTAL = 16;
export const APP_PADDING_TOP = 16;
